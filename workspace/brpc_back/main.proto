syntax = "proto3";
package example;

option cc_generic_services = true; // 这个一定不要忘记, 不然不会生成 EchoService

message EchoRequest 
{
    string message = 1;
}

message EchoResponse 
{
    string message = 1;
}

service EchoService 
{
    rpc Echo(EchoRequest) returns (EchoResponse);
};

